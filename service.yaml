apiVersion: v1
kind: Service
metadata:
  name: star-agile-banking-project
spec:
  type: NodePort
  selector:
    app: star-agile-banking-project
  ports:
  - port: 80
    targetPort: 8081
    nodePort: 30082
